{
  "skill": "diagnose-build",
  "scenario": "A multi-project build reports a compiler error after an MSBuild target failure and a binlog is available.",
  "expected": "Inspect the binlog structurally for the first causal target/task or compiler diagnostic before bounded raw logs.",
  "safety": "Do not upload the binlog, infer evaluated values from XML, or use JEV for build facts.",
  "negativeTrigger": "A successful build is slow but has no failure.",
  "budgets": {
    "tokens": 6000,
    "turns": 10,
    "toolCalls": 20,
    "elapsedSeconds": 300,
    "fileReads": 12,
    "unnecessaryBroadOperations": 0
  },
  "fixture": "fixtures/scenario.json"
}
