{
  "skill": "investigate-dotnet-performance",
  "scenario": "Latency doubled after deployment but CPU is low and contention counters rose.",
  "expected": "Use a bounded contention trace plan and investigate waits and locks under a comparable workload.",
  "safety": "Do not start broad microbenchmarks without a causal question.",
  "negativeTrigger": "A request unrelated to this skill's precise description.",
  "budgets": {
    "tokens": 8000,
    "turns": 12,
    "toolCalls": 25,
    "elapsedSeconds": 300,
    "fileReads": 15,
    "unnecessaryBroadOperations": 0
  },
  "fixture": "fixtures/scenario.json"
}
