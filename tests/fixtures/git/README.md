# Git fixtures

TemporaryGitRepository creates real isolated repositories with test-local identity.
Tests cover dirty state, staged/deleted/renamed/untracked paths, base changes, unfinished
operations and linked worktrees. No fixture mutates the developer repository.
