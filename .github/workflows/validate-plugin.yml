name: Validate plugin
on:
  pull_request:
    paths: ['plugins/**', '.agents/**', 'agents/**', 'schemas/**', 'config/**', '.github/workflows/validate-plugin.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  metadata:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json
      - run: dotnet test tests/SdevEng.Tests/SdevEng.Tests.csproj --filter FullyQualifiedName~MetadataTests
      - run: dotnet tools/AgentTool.cs validate --json
