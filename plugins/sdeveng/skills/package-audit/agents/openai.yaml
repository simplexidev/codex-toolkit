interface:
  display_name: "Package Audit"
  short_description: Audit .NET package vulnerability metadata on explicit request.
  default_prompt: "Use $package-audit."
policy:
  allow_implicit_invocation: false
