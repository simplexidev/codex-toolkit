interface:
  display_name: "Address Pr Review"
  short_description: Address concrete pull request review comments after a review arrives.
  default_prompt: "Use $address-pr-review."
policy:
  allow_implicit_invocation: true
