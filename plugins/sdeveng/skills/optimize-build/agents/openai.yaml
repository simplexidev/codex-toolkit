interface:
  display_name: "Optimize Build"
  short_description: Optimize a measured slow .NET build, evaluation, project graph, incremental build, or parallel execution.
  default_prompt: "Use $optimize-build."
policy:
  allow_implicit_invocation: true
