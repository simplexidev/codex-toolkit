---
name: jev-judgment
description: Use bounded semantic Choice, Noul or Score judgment to reduce a large ambiguous candidate set.
---

# Jev Judgment

Try deterministic filtering first. Declare a configured `capability`, allowed `purpose`, and `deterministicNarrowed: true` in the local input; never invent a purpose to bypass a zero budget. Read references/primitives.md only for request shape and references/thresholds.md for calibration. Use `jev TYPE --input sanitized.json --dry-run` to inspect the payload. Actual transmission uses `--safe-input` after minimizing and reviewing the text. Never request, store, echo, inspect, or inject credentials; a live call is allowed only when AgentTool is already narrowly configured externally. Off, unavailable credentials, invalid responses, timeouts and uncertainty route to GPT; never discard REVIEW. Inspect structured instrumentation for budget use, confidence, fallback, escalation, and context avoided without copying payloads into logs. No code generation, architecture, open-ended debugging, prose, calculations, Git mutations, exact facts/commands, or authorization/security decisions.
