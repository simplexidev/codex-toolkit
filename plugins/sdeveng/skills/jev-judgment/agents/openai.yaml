interface:
  display_name: "Jev Judgment"
  short_description: Use bounded semantic Choice, Noul or Score judgment to reduce a large ambiguous candidate set.
  default_prompt: "Use $jev-judgment."
policy:
  allow_implicit_invocation: false
