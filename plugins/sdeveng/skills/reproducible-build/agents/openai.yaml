interface:
  display_name: "Reproducible Build"
  short_description: Audit or verify deterministic .NET build reproducibility on demand.
  default_prompt: "Use $reproducible-build."
policy:
  allow_implicit_invocation: false
