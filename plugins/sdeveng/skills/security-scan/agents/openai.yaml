interface:
  display_name: "Security Scan"
  short_description: Run or triage deterministic security analyzers and SARIF for a scoped change.
  default_prompt: "Use $security-scan."
policy:
  allow_implicit_invocation: true
