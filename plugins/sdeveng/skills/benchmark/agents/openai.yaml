interface:
  display_name: "Benchmark"
  short_description: Create or run an explicitly requested .NET performance benchmark.
  default_prompt: "Use $benchmark."
policy:
  allow_implicit_invocation: false
