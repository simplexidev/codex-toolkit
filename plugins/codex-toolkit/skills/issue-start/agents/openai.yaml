interface:
  display_name: "Issue Start"
  short_description: "Start work on a specific open GitHub issue with a safe task b..."
  default_prompt: "Use $issue-start for the scoped task and return evidence."
policy:
  allow_implicit_invocation: false
