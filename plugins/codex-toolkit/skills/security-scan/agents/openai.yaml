interface:
  display_name: "Security Scan"
  short_description: "Run or triage deterministic security analyzers and SARIF for ..."
  default_prompt: "Use $security-scan for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
