interface:
  display_name: "Dependency Change"
  short_description: "Change .NET package references or central package versions sa..."
  default_prompt: "Use $dependency-change for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
