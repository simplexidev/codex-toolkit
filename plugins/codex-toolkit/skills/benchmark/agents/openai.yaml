interface:
  display_name: "Benchmark"
  short_description: "Create or run an explicitly requested .NET performance benchm..."
  default_prompt: "Use $benchmark for the scoped task and return evidence."
policy:
  allow_implicit_invocation: false
