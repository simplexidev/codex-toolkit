interface:
  display_name: "Diagnostics"
  short_description: "Collect or inspect .NET runtime counters, traces, dumps or GC..."
  default_prompt: "Use $diagnostics for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
