interface:
  display_name: "Docs Impact"
  short_description: "Assess documentation changes required by a code, configuratio..."
  default_prompt: "Use $docs-impact for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
