interface:
  display_name: "Roadmap Next"
  short_description: Identify an actionable next issue from explicit roadmap and dependency metadata.
  default_prompt: "Use $roadmap-next."
policy:
  allow_implicit_invocation: true
