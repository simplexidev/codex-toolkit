interface:
  display_name: "Roadmap Next"
  short_description: "Identify an actionable next issue from explicit roadmap and d..."
  default_prompt: "Use $roadmap-next for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
