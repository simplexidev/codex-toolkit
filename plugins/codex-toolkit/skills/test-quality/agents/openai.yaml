interface:
  display_name: "Test Quality"
  short_description: "Assess targeted .NET test coverage, assertions or optional mu..."
  default_prompt: "Use $test-quality for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
