interface:
  display_name: "Package Audit"
  short_description: "Audit .NET package vulnerability metadata on demand or after ..."
  default_prompt: "Use $package-audit for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
