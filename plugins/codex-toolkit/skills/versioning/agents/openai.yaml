interface:
  display_name: "Versioning"
  short_description: Apply an existing Git-derived .NET versioning policy for a release or package change.
  default_prompt: "Use $versioning."
policy:
  allow_implicit_invocation: true
