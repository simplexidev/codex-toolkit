interface:
  display_name: "Versioning"
  short_description: "Apply an existing Git-derived .NET versioning policy for a re..."
  default_prompt: "Use $versioning for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
