interface:
  display_name: "Dotnet Format"
  short_description: "Format changed C# files or verify their formatting."
  default_prompt: "Use $dotnet-format for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
