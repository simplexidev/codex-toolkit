interface:
  display_name: "Architecture Change"
  short_description: Plan or review a real change to component boundaries or system architecture.
  default_prompt: "Use $architecture-change."
policy:
  allow_implicit_invocation: true
