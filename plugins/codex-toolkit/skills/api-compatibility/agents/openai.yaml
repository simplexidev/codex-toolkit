interface:
  display_name: "Api Compatibility"
  short_description: "Check public .NET API compatibility after an actual public su..."
  default_prompt: "Use $api-compatibility for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
