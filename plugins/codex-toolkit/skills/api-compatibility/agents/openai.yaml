interface:
  display_name: "Api Compatibility"
  short_description: Check public .NET API compatibility after an actual public surface change.
  default_prompt: "Use $api-compatibility."
policy:
  allow_implicit_invocation: true
