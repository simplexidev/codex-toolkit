interface:
  display_name: "Repo Health"
  short_description: Audit repository build and .NET configuration against toolkit policy.
  default_prompt: "Use $repo-health."
policy:
  allow_implicit_invocation: true
