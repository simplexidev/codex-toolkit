interface:
  display_name: "Repo Health"
  short_description: "Audit repository build and .NET configuration against toolkit..."
  default_prompt: "Use $repo-health for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
