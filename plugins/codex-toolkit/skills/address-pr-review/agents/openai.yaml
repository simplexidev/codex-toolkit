interface:
  display_name: "Address Pr Review"
  short_description: "Address concrete pull request review comments after a review ..."
  default_prompt: "Use $address-pr-review for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
