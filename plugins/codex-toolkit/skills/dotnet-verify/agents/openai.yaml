interface:
  display_name: "Dotnet Verify"
  short_description: "Validate a .NET change using affected project and test depend..."
  default_prompt: "Use $dotnet-verify for the scoped task and return evidence."
policy:
  allow_implicit_invocation: true
