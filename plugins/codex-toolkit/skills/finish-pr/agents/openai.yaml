interface:
  display_name: "Finish Pr"
  short_description: Prepare an authorized push and pull request after implementation is ready.
  default_prompt: "Use $finish-pr."
policy:
  allow_implicit_invocation: false
