interface:
  display_name: "Finish Pr"
  short_description: "Prepare an authorized push and pull request after implementat..."
  default_prompt: "Use $finish-pr for the scoped task and return evidence."
policy:
  allow_implicit_invocation: false
