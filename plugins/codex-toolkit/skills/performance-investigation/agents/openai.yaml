interface:
  display_name: "Performance Investigation"
  short_description: Investigate an observed .NET latency, CPU, allocation or memory regression.
  default_prompt: "Use $performance-investigation."
policy:
  allow_implicit_invocation: true
