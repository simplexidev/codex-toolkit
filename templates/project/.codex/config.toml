# Project-only Codex settings. Models and workflow defaults stay in user configuration.
# Add project-specific settings here only when required by this repository.
[agents]
max_concurrent_threads_per_session = 3
