# Agent integration

Install the central toolkit once, then fill project-local AGENTS.md with architecture,
validation targets and constraints. Merge .codex/config.toml with any existing settings.
The toolkit's instructions and skills stay in the central checkout. Review optional
`.NET` defaults in `dotnet/` before adopting them; do not overwrite established project
policy.
